# Coverage progress observability

Make the deliberately slow historical backfill measurable without adding source
requests or changing its off-peak schedule. Health reports must expose the
configured horizon, target records, remaining records, and target status.

Sigstore signing and production Zenodo DOI publication remain separate gated
items; this track does not enable either external publication path.
