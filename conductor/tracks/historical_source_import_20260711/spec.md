# Historical source import

The archive may use a historical Morph export and Internet Archive CDX export
as evidence inputs when live Right to Know access is unavailable. These inputs
must remain separately attributed and must not be presented as live captures.

The importer is offline-only, records input checksums and local retrieval times,
and deduplicates records by canonical source URL.
